{
  "name": "version-manager",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm i && npm run build && npx http-server ./src/build --cors",
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "clean": "git clean -dfx"
  },
  "devDependencies": {
    "@types/slickgrid": "~2.1.32",
    "typescript": "~4.9.4"
  }
}
