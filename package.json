{
  "name": "version-manager",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm i && npm run build && npm run serve",
    "clean": "git clean -dfx",
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "serve": "npx -y http-server@latest ./src/build --cors"
  },
  "devDependencies": {
    "@types/slickgrid": "~2.1.32",
    "typescript": "~4.9.4"
  }
}
